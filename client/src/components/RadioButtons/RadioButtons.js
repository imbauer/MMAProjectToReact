import React, { Component } from 'react';
import './NavBar.css';

class RadioButtons extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <button>Update Parent</button>
        );
    }
}

export default RadioButtons;
